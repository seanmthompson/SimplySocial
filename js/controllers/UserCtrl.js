define([], function () {
    var controllerName = 'ssUserCtlr';
    var fn = function ($rootScope, $scope, $window, $compile, $location, $routeParams, $timeout, ssCommonService, ssDataService) {
	    	    
    };

    angular.module('ssApp').lazy.controller(controllerName, fn);
}); 